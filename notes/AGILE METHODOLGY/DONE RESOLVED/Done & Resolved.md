This part it's the last step of the process; when we get here is because our code is already merged into production. 
Achieving the state of "done/resolved" is the objective we aim for in the every project to fulfill all of its goals.
